import styled from "styled-components";

export const StyledDashboard = styled.div`
    padding: 16px;
    .dash-header {
        font-size: 18px;
        span {
            color: #9a6aff;
            font-weight: 700;
        }
    }
`