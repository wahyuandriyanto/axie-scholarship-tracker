import styled from "styled-components";

export const StyledLayout = styled.div`
    background-color: #fff;
    max-width: 30rem;
    width: 100%;
    min-height: 100vh;
    overflow-x: hidden;
    overflow-y: scroll;
    margin: 0 auto;
    position: relative;
    ::-webkit-scrollbar {
    display: none;
}
`